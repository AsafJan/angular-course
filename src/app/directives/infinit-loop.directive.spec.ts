import { InfinitLoopDirective } from './infinit-loop.directive';

describe('InfinitLoopDirective', () => {
  it('should create an instance', () => {
    const directive = new InfinitLoopDirective();
    expect(directive).toBeTruthy();
  });
});
